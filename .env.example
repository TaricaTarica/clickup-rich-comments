# Copy to .env for local reference only — do NOT commit .env
# The installer saves CLICKUP_API_TOKEN to your shell profile instead.
#
# Get your token: ClickUp → Settings → Apps → API Token
CLICKUP_API_TOKEN=pk_your_personal_api_token_here
